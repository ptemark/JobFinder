### IntelliJ IDEA ###
out/
!**/src/main/**/out/
!**/src/test/**/out/
.kotlin

### Eclipse ###
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache
bin/
!**/src/main/**/bin/
!**/src/test/**/bin/

### NetBeans ###
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/

### VS Code ###
.vscode/

### Mac OS ###
.DS_Store

### Python / Job Finder ###
# Byte-compiled / build artifacts
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
# Virtual envs
.venv/
venv/
# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
# Local data, caches and logs (never leave the machine — Cost & Safety §5)
data/
logs/
# Secrets and personal data (never committed — Cost & Safety §4)
.env
config/resume.*
# M7 Google Sheets service-account key (never committed — LLD §11.3)
config/google-service-account.json
# Personal résumé kept at repo root (belongs in config/resume.* per spec)
/Peter_Mark_Resume.*
# Local, user-specific generated config (examples are committed)
config/profile.yaml
config/companies.yaml
config/weights.yaml