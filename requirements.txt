# Pinned dependency target for Job Finder (LLD §14).
#
# The live, locked environment is managed by uv (pyproject.toml + uv.lock); see
# RALPH.md. Dependencies are added incrementally with `uv add` as each task first
# needs them, so pyproject.toml may list a subset of the entries below at any given
# point in the build. This file documents the full intended pinned set and lets a
# plain `pip install -r requirements.txt` reproduce the runtime environment.

httpx[http2]>=0.27,<1.0   # http2 extra (h2) required by the LLD §3.2 http2=True client
selectolax>=0.3,<0.4
sentence-transformers>=3.0,<4.0   # pulls torch (CPU), transformers — the one heavyweight
numpy>=1.26,<3.0
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
typer>=0.12,<1.0
PyYAML>=6.0,<7.0
pypdf>=4.0,<6.0
pdfplumber>=0.11,<1.0     # fallback resume extraction
python-docx>=1.1,<2.0

# Dev / test only (managed via uv dev group):
pytest>=8.0
ruff>=0.4
