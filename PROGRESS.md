# Progress — Job Finder

Milestone-by-milestone status, updated each Ralph iteration. The authoritative
task list and acceptance checks live in `spec/tasks.md`; this file is the
human-readable rollup.

## Milestone status

| Milestone | Scope | Status |
|-----------|-------|--------|
| M1 — Skeleton + DB | scaffold, settings, models, SQLite store | done |
| M2 — Sources + normalizer | http client, Greenhouse/Lever, normalize | done |
| M3 — Resume + filters + scoring | extraction, embeddings, scoring math | in progress |
| M4 — Dashboard | FastAPI API + static SPA | not started |
| M5 — Ashby + Adzuna + discovery | extra sources, board-token harvest | not started |
| M6 — Polish | CLI, README, export, hardening | not started |

## Task log

| Task | Status | Notes |
|------|--------|-------|
| T16 — Scoring math & weights | done | `score_job(job, profile_vec, job_vec, *, profile, weights, now) -> ScoreBreakdown` (LLD §6.3–§6.4) — pure + model-free: takes the two pre-computed L2-normalized vectors (the pipeline does embed→score in one step; split here so the ranking test is deterministic & offline). Components: `semantic` = cosine clamped to [0,1]; `skill` = fraction of {java,kotlin,python,aws} matched word-boundary/case-insensitive (`\bjava\b` ≠ "javascript"), saturating at 1.0; `location` = bonus map {remote 1.0, vancouver 0.85, toronto 0.7, other_canada 0.4, other 0.0}; `recency` = linear `clamp(1 - age/max_age_days)` with date_unknown→0.3 (so undated jobs still rank). Final = weight-normalized sum → `round(100·final01, 1)`; denominator > 0 guaranteed by the `Weights` validator. `weights.yaml.example` carries the §6.4 defaults (0.35/0.30/0.20/0.15). Both load-bearing ordering tests pass: skill weight flips an off-stack role that has the *higher* cosine below a Java/AWS role; senior remote Java/AWS outranks junior onsite frontend end-to-end through the real model. **Recovery:** prior iteration committed this under a mislabeled message (`00246d3`) with `test_score.py` ruff-format-dirty (CI red) and T16 still `[~]`; this iteration reformatted (whitespace-only), reran CI green, marked `[x]`. No new deps. CI green (161 tests, ruff + format clean). **M3 complete; T17 pipeline next.** |
| T15 — Embeddings & profile vector | done | `score.py` (LLD §6.1–§6.3): `load_model(name)` caches `SentenceTransformer` instances in a module dict, lazy-importing `sentence_transformers`/torch inside the call so résumé extraction never pays the heavy import. `build_profile_vector(profile, resume_text, *, model)` prepends `render_targeting(profile)` (role+skills+seniority, so stated priorities steer the vector) to the full résumé, chunks via pure `_chunk_text` into ≤180-word windows (≈256 tokens — conservative, keeps tail off the model's truncation limit), encodes all chunks (`normalize_embeddings=True`), mean-pools, then `_l2_normalize`s to unit length. `embed_job(job, *, model)` char-caps `title+description` to 5000 then encodes+normalizes. Both take an injected `Encoder` Protocol so the chunk/pool math is offline-testable with a deterministic fake; the dim(384)/unit-norm/determinism checks use the real model via a session-scoped `embed_model` fixture in new `tests/conftest.py` (downloaded once — the sanctioned T15 network carve-out — then offline, reused by T16). 13 new tests (chunk split+tail+empty, targeting block, long-résumé chunking via recording fake, dim/norm, determinism ×2, embed_job norm/determinism/char-cap, load_model caching via monkeypatched ST). Deps: `sentence-transformers`, `numpy` (LLD §14 — the one heavyweight, justified by core matching). CI green (145 tests, ruff clean). |
| T14 — Resume extraction | done | `extract_resume(path) -> str` (LLD §6.5): suffix dispatch — `.txt`/`.md` read UTF-8; `.docx` via python-docx (paragraphs **then** table cells, in order); `.pdf` via pypdf, falling back to pdfplumber when pypdf yields no non-whitespace text. Missing file → `FileNotFoundError`; unsupported existing suffix → `ValueError`. Heavy extractors lazy-imported per branch so `score.py` import stays cheap (no torch). Fixtures `tests/fixtures/resume.{txt,md,docx,pdf}` are a senior-backend CV with Java/Kotlin/Python/AWS (reused by T15/T16); hand-built minimal PDF (no PDF-writing dep) + docx skills table. 8 offline tests incl. forced pdfplumber-fallback via monkeypatch + sad paths. Deps: pypdf, pdfplumber, python-docx (LLD §14). **M3 started.** |
| T01 — Repo scaffold & packaging | done | uv project; `jobfinder` entry point wired to no-op Typer `app`; deps added per-task (see RALPH.md), full pinned target in requirements.txt (LLD §14); Python pinned to 3.12 for later torch CPU wheels. |
| T02 — Settings & config loading | done | pydantic-settings `Settings` (env + `.env`, `JOBFINDER_*` prefix, paths from `base_dir`); Adzuna secrets via unprefixed `.env` aliases, `adzuna_enabled` gated on both keys. `Profile`/`Weights`/`CompaniesConfig` models with fail-fast `load_*` helpers; `*.example` configs + `.env.example` shipped. 14 tests (valid/malformed/missing-secret). |
| T03 — Core data models | done | `RawPosting` (frozen), `Job`, `ScoreBreakdown` dataclasses + `LocationBucket`/`Seniority`/`Status` enums per LLD §2; `StrEnum` (ruff UP042-mandated, modern equivalent of the LLD's `(str, Enum)`) so members round-trip to the TEXT columns. Stable id via `make_job_id` = `sha1("{source}:{source_id}")[:16]` (+ `Job.make_id` alias). 10 tests: id stability/distinctness/length, enum round-trips, frozen RawPosting, dataclass defaults. |
| T04 — SQLite schema & connection | done | `connect()` applies LLD §7.1 PRAGMAs (WAL, synchronous=NORMAL, busy_timeout=5000, foreign_keys=ON) + `sqlite3.Row` + auto parent-dir; `init_db()` runs the full §7.2 DDL via `executescript` (all `IF NOT EXISTS` → idempotent). 5 tests (PRAGMAs on file db, parent-dir, all tables/indexes, idempotent re-run, UNIQUE dup rejected). No new deps. |
| T05 — Job upsert & dedupe | done | `upsert_job` = `INSERT ... ON CONFLICT(source, source_id) DO UPDATE` (LLD §7.3): idempotent re-poll preserves `first_seen_at` (omitted from SET), bumps `last_seen_at`, refreshes mutable fields incl. `embedding`/`eligible`/`ineligible_reason`/`content_hash`. `_job_params` coerces bool→int, StrEnum→value, datetime→ISO, `raw`→JSON. Added `eligible`/`ineligible_reason`/`content_hash` to `Job` (DDL §7.2 + pipeline §8 require them; LLD §2 listing abbreviates them out). 2 new tests (insert+coercion, dedupe idempotency). M1 store layer continues in T06. |
| T06 — Scores/status/runs/companies/prune DAL | done | Remaining LLD §7.3 ops: `save_score` & `set_status` upsert on their PK (re-score/re-status replace, never duplicate); `start_run`/`finish_run` open then close a `poll_runs` row (`started_at`, then `finished_at`+`per_source_json` funnel); `add_company` = `ON CONFLICT(ats,token) DO NOTHING` (discovery dedup, never downgrades a verified entry) + `get_companies` reader; `prune(not_seen_days)` deletes by lexicographic ISO `last_seen_at < cutoff`, returns rowcount, cascades scores/status via FK. `now` injectable on all clock-using ops for deterministic tests. 6 new tests (score upsert, cascade delete, status upsert, run bookkeeping, company dedup/preserve, prune+cascade). **M1 store layer complete.** No new deps (stdlib). |
| T08 — Source protocol & registry | done | LLD §3.1: `SourceResult` dataclass + `Source` runtime_checkable `Protocol` (`name`, `fetch(*, max_age_days, throttle_s)`) + module-global `SOURCES` registry with `register_source` (adapters self-register at import; T11/T12/T21/T22) and `build_sources(settings, *, only, registry)`. `only` honors the CLI `--source` selection and fails fast on unknown names; `registry` injectable for test isolation. Per LLD, name-selection here is separate from secret-skip: an optional keyed source (Adzuna) is constructed without its key and `fetch` returns empty + note, never raises (HLD §5.1). 10 offline tests. No new deps. |
| T07 — Shared HTTP client (throttle, retry, cache) | done | `HttpClient` over one `httpx.Client` (LLD §3.2): timeouts 10s/connect 5s, http2=True, descriptive UA. Per-host monotonic throttle (≥`throttle_s`); ≤3-attempt retry on `{429,500,502,503,504}`+connect/read timeouts (`0.5*2**n`+jitter, honors integer `Retry-After` on 429); on-disk JSON cache key=`sha1(full-url)` under `data/http_cache/`, wall-clock TTL, hit skips network+throttle, `no_cache` bypass. All time/IO seams injectable → 14 offline deterministic tests. Module-level `get_json`/`get_text` delegate to a lazy `Settings`-built default client (`configure/reset_default_client` for CLI/test). Dep: `httpx[http2]` (h2 needed by http2=True; already in LLD §14 target). **M2 started.** |
| T10 — Normalizer: location bucketing & seniority | done | Added `bucket_location`/`infer_seniority`/`normalize` to `normalize.py` (LLD §4.1–§4.3). `bucket_location` ordered rules: remote signal (source flag OR `/remote/i`) → REMOTE, except non-Canada-only remote (`/remote.*(us only\|united states only\|emea)/i`) → OTHER; then Vancouver/Toronto/other-Canada metros/else OTHER; returns the effective `is_remote` alongside. `infer_seniority` first-match-wins on title: people-manager/exec (`principal\|director\|vp\|head of\|manager\b`) → UNKNOWN unless clearly IC (`staff\|principal engineer`) → STAFF; then staff/senior(`sr.`/`lead`)/junior/mid; generic title falls back to unambiguous senior/junior cues in the body (numeric mid cues never read from prose). `normalize` dispatches per-source field extraction (greenhouse: entity-decode `content` via `html.unescape`→`html_to_text`, `company_name`/hint; lever: `descriptionPlain`, company from hint) then applies the helpers + sets `date_unknown`; unknown source raises (Ashby/Adzuna extractors land with T21/T22). 25 new tests (10 bucket branches + remote-flag/remote-wins, 13 seniority titles + desc-fallback + numeric-prose-ignored, greenhouse/lever normalize + date_unknown + unknown-source raise). No new deps. |
| T11 — Greenhouse adapter | done | `GreenhouseSource` (LLD §3.3): fetches each configured greenhouse board (`/v1/boards/{token}/jobs?content=true`, no auth) via the shared `HttpClient`, returns `RawPosting`s with the verbatim payload. **Recency pre-filter applied before returning** — no server-side date filter, so postings with `updated_at` older than `max_age_days` are dropped here so they never reach normalize/embed/score; a date-unknown posting (null `updated_at`) is kept (flagged downstream), never silently dropped. Per-board bulkhead: an HTTP error / bad JSON / shape mismatch on one board is logged + recorded in `errors` and skips that board only; a malformed/id-less posting is skipped+noted but counted in `fetched`. `now` injectable for deterministic recency tests. Self-registers via `register_source` on import (package `__init__` imports the module). Fixture `tests/fixtures/greenhouse_jobs.json` (fresh/stale/date-unknown/id-less rows); 5 offline tests (parse+fetched/error counts, recency drop, RawPosting→normalize round-trip, per-board error isolation, unexpected shape). No new deps. |
| T12 — Lever adapter | done | `LeverSource` (LLD §3.4): fetches each configured lever site (`/v0/postings/{site}?mode=json&limit=100`, no auth) via the shared `HttpClient`, returns `RawPosting`s with the verbatim payload. Lever returns a JSON **array** and paginates via `skip`/`limit`, so `_fetch_site` walks pages until a short (final) page, with a `LEVER_MAX_PAGES` cap; `page_limit` is a constructor arg so paging is testable. **Recency pre-filter before returning** — `createdAt` epoch-ms older than `max_age_days` dropped pre-normalize; null/absent `createdAt` kept (flagged date_unknown). Company name absent from Lever payloads → supplied at normalize via `company_hint` from `companies.yaml` (no normalize change). Per-site bulkhead: HTTP error / bad JSON / non-array shape logged + recorded, skips that site only; id-less/non-object posting skipped+noted but counted in `fetched`. `now` injectable. Self-registers on import (package `__init__` now imports `lever`). Fixture `tests/fixtures/lever_postings.json` (fresh/stale/date-unknown/id-less); 6 offline tests (parse+counts, recency drop, normalize round-trip incl. company-hint/epoch-ms/descriptionPlain+HTML-fallback, pagination stop-on-short-page, site-error isolation, non-array shape). No new deps. |
| T09 — Normalizer: HTML, dates, helpers | done | New pure module `normalize.py` (no I/O, LLD §4.3). `html_to_text` (selectolax): drops `script`/`style`, decodes entities via `.text`, separator keeps adjacent blocks apart, `str.split` collapses all whitespace incl. `\xa0` from `&nbsp;`; empty/whitespace → `""`. `parse_date(value, source)` dispatches on source: epoch-ms sources (`lever`) parsed from int/float/numeric-str via `fromtimestamp(v/1000, tz=UTC)`; all others ISO8601 via `fromisoformat` (handles `Z`, naive→UTC assumed, else `astimezone(UTC)`); any failure → `None` (caller sets `date_unknown`). `bool` rejected as a non-timestamp. 14 tests (tag/entity strip, nbsp collapse, block separation, empty; ISO offset/Z/naive; epoch int/str; None/garbage/bool sad paths). No new deps (selectolax already pinned LLD §14). |

## TODO verify (real-world unknowns to confirm)

- **Company board tokens** in `config/companies.yaml.example` are seeded as
  plausible Canadian/remote employers but the exact ATS slugs are UNCONFIRMED
  (`# TODO verify`): greenhouse `shopify`/`benevity`/`clio`, lever
  `jobber`/`thinkific`, ashby `wealthsimple`. Confirm each against the live feed
  and flip `verified: true`.
